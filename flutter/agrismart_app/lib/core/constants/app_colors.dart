import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  static const Color primary = Color(0xFF00FF2A);
  static const Color primaryDark = Color(0xFF139D60);

  // Background Colors
  static const Color background = Color(0xFF0F1B12);
  static const Color backgroundDark = Color(0xFF08160E);

  // Surface Colors
  static const Color surface = Color(0xFF162519);
  static const Color surfaceBorder = Colors.white12;

  // Text Colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFF8E9B92);
  static const Color textMuted = Colors.white54;
}
